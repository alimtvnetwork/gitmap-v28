# gitmap servers-clients ls

List all server and client nodes in the cluster.

## Usage

```bash
gitmap servers-clients ls [--json]
```

## Examples

```bash
gitmap servers-clients ls
gitmap servers-clients ls --json
```

See also: `gitmap servers ls`, `gitmap clients ls`, `gitmap cluster nodes`
