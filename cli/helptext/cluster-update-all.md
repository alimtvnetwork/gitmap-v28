# gitmap cluster update-all

Update the gitmap executable across all reachable cluster nodes simultaneously.

## Usage

```bash
gitmap cluster update-all [--version <ver>] [--except <list>]
```

## Examples

```bash
gitmap cluster update-all
gitmap cluster update-all --except 2
```

See also: `gitmap cluster update`, `gitmap update`
