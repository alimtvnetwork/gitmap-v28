# gitmap cluster cat

Read file content from a remote cluster node.

## Usage

```bash
gitmap cluster cat --id <node-id> <remote-file-path>
```

## Examples

```bash
gitmap cluster cat --id 2 "C:\app\config.json"
```

See also: `gitmap cluster write`, `gitmap cluster`
