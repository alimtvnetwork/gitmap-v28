package constants

// gitmap:cmd top-level
// MultiClone batch cloning command and aliases.
const (
	// CmdMultiClone is the primary verb for batch multi-repo cloning.
	CmdMultiClone = "multiclone"

	// CmdMultiCloneAlias is the canonical short alias for multiclone.
	CmdMultiCloneAlias = "mc"

	// CmdMutliCloneAlias handles the common typo alias explicitly requested by users.
	CmdMutliCloneAlias = "mutliclone"
)
