{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BookmarkList",
  "description": "Array of saved command bookmarks emitted by `gitmap bookmark list --json`",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "id",
      "name",
      "command"
    ],
    "properties": {
      "id": {
        "type": "integer",
        "description": "SQLite row ID"
      },
      "name": {
        "type": "string",
        "description": "User-defined bookmark name"
      },
      "command": {
        "type": "string",
        "description": "The gitmap command being bookmarked"
      },
      "args": {
        "type": "string",
        "description": "Positional arguments stored with the bookmark"
      },
      "flags": {
        "type": "string",
        "description": "Flag string stored with the bookmark"
      },
      "createdAt": {
        "type": "string",
        "description": "ISO-8601 timestamp of when the bookmark was created"
      }
    }
  }
}
