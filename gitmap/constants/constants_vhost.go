package constants

// gitmap:cmd top-level
// Virtual host CLI commands.
const (
	CmdVHost = "vhost"
)

// Virtual host help text.
const HelpVHost = "  vhost                  Manage Nginx virtual hosts (list, create, enable, disable, test, reload)"
