# gitmap clients ls

List all registered client nodes in the cluster.

## Usage

```bash
gitmap clients ls [--json]
```

## Examples

```bash
gitmap clients ls
gitmap clients ls --json
```

See also: `gitmap servers ls`, `gitmap servers-clients ls`, `gitmap cluster nodes`
