# gitmap cluster update

Update the gitmap executable on a specific cluster node.

## Usage

```bash
gitmap cluster update --id <node-id> [--version <ver>]
```

## Examples

```bash
gitmap cluster update --id 2
gitmap cluster update --id 3 --version v6.99.0
```

See also: `gitmap cluster update-all`, `gitmap update`
