# gitmap cluster set-default-path

Configure the default execution directory for a cluster node.

## Usage

```bash
gitmap cluster set-default-path --id <id> --path <directory>
```

## Examples

```bash
gitmap cluster set-default-path --id 2 --path "C:\Projects"
```

See also: `gitmap cluster set-path-alias`, `gitmap cluster`
