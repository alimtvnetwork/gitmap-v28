# snapshot

Snapshot the full working tree to `.gitmap/snapshot/snap-<UTC-ts>.tar.gz`. Broader than `undo` — captures untracked files too.

## Examples

```bash
gitmap snapshot
gitmap snapshot path/to/repo
```
