# gitmap servers ls

List all registered server nodes in the cluster.

## Usage

```bash
gitmap servers ls [--json]
```

## Examples

```bash
gitmap servers ls
gitmap servers ls --json
```

See also: `gitmap clients ls`, `gitmap servers-clients ls`, `gitmap cluster nodes`
