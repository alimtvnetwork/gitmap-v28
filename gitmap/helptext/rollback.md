# rollback

Restore the latest `gitmap snapshot` tarball (or a specific one) over the current working tree.

## Examples

```bash
gitmap rollback
gitmap rollback .gitmap/snapshot/snap-20260628-120000.tar.gz
```
