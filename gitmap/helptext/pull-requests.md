# pr

List open pull requests across all your GitHub repos in a single table. Uses `GITHUB_TOKEN` if set for higher rate limits and private repo access.

## Examples

```bash
gitmap pr
gitmap pr some-owner
```
