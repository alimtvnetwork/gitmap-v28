# gitmap uzc

Shorthand for `gitmap unzip-compact`. See [`unzip-compact`](unzip-compact.md)
for the full command reference, flag list, normalization rules, and
format coverage.

## Scripting (JSON)

Discover this command from a script using the machine-readable help payload:

```bash
gitmap help --json --filter uzc
```

The JSON schema is published at `spec/08-json-schemas/help-json.schema.json` (v5.43.0+).

## Examples

```bash
gitmap uzc
```
