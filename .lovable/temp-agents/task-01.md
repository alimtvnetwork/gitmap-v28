STATUS: DONE

Task 1: UI Terminal Frontend has been successfully completed.
- Created `src/components/terminal/TerminalView.tsx` utilizing `@xterm/xterm` to implement the web-based terminal UI.
- Implemented high contrast MULTI-OS THEMES: win (Cmd blue), ubuntu (dark eggplant `#300a24`), linux (black with bright green text).
- Added `pb-20` (5rem) padding at the bottom of the active tab content area to give significant visual breathing room.
- Features include multiple terminal tabs, and `hasSplits` prop coupled with `react-resizable-panels` implementation to allow multiple splits.
- Complied strictly with 15-line max function size limits and strict boolean name rules (no negative variables).
