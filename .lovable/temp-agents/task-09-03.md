Task 03 completed successfully.
- Rewrote `gitmap/cmd/open.go` to use `xdg-open`, `open`, and `rundll32 url.dll,FileProtocolHandler` depending on `runtime.GOOS`.
- Kept `runOpen(args []string) error` with the fallback to `gitTopLevel()` -> `os.Getwd()` -> `filepath.Abs()`.
- Updated `constants_open.go` to remove old desktop/vscode messages and added `HelpCmdOpen` with simpler helptext.
- Verified `CmdOpen` mapping in `roottooling.go` and `constants_cli.go`.
- Added new OS-specific documentation in `gitmap/cmd/helptext/open.md`.
- `go build ./...` ran successfully.
