# Subtask Naming Convention

Subtasks must NEVER be prefixed with 'SS-' or 'SS-XX-'.
They must strictly follow the 'XX-<slug>.md' sequence (where XX is a zero-padded number like 01, 02).

The project strictly follows the coding guidelines and this naming convention is enforced across all tasks.
