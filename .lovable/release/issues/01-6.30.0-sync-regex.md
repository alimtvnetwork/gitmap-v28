- **Previous version**: 6.29.0
- **New version**: 6.30.0
- **Step that failed**: Step 7 - Verify version sync
- **Command run and full error output**: 
  `npx vitest run src/test/version-sync.test.ts`
  Error: `AssertionError: Version constant not found in D:\work\gitmap\gitmap\constants\constants.go`
  `bash .github/scripts/check-changelog-version-sync.sh`
  Error: `✗ Could not parse Version from $CONSTANTS_FILE` and `✗ CHANGELOG drift: constants.Version is 6.30.0 but no matching '## v6.30.0' heading exists`
- **Files involved**: `.github/scripts/check-changelog-version-sync.sh`, `src/test/version-sync.test.ts`
- **Resolution or workaround**: Updated the regular expressions in both scripts. Changed the Go constants regex to match `var Version` instead of `const Version` (as it was previously changed to support `-ldflags`). Also updated the Bash script regex `HEADING_RE` to allow optional brackets `\[?v...\]?` so it can match `## [v6.30.0]` properly.
