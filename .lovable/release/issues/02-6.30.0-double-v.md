- **Previous version**: 6.29.0
- **New version**: 6.30.0
- **Step that failed**: CI/CD Installer Smoke Test
- **Command run and full error output**: 
  `bash .github/scripts/smoke-installer.sh release`
  Error: `Error: Version mismatch. expected: gitmap v6.30.0, actual: gitmap vv6.30.0`
- **Files involved**: `.github/workflows/release.yml`, `gitmap/constants/constants_messages.go`
- **Resolution or workaround**: The `LDFLAGS` in `release.yml` was injecting the version with a `v` prefix (`v6.30.0`), which caused `gitmap version` (which hardcodes `gitmap v%s`) to print `vv6.30.0`. Fixed `release.yml` to inject `$CLEAN_VERSION` (without the `v` prefix) into `constants.Version` and `gitmap-updater/cmd.Version`. Deleted the remote tag `v6.30.0` and re-pushed to trigger a fresh build.
