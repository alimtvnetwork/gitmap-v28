# Subtask 26-03: Pattern Matching & Helper Methods

## Goal

Provide compile-time exhaustive switch checks and validator methods (`IsValid`, `IsTerminal`, `assertNever`).

## Status: DONE

- Implemented `assertNever` in `src/types/result.ts` and validated exhaustive enum helpers.
