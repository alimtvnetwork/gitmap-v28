# Subtask 4: Help Routers and Wiring

1. Create `gitmap/cmd/find_entry.go`:
   - Implement `runFind`, `runFindRegex`, `runFindRead`, `runFindReadJson`, `runFindRegexRead`, `runFindRegexReadJson`.
   - Implement `runFindHelp`, `runSearchHelp`, `runRegexHelp` to print groups of constants using `pterm`.
   - Parse `--limit` flag dynamically from arguments.
2. Run `go test` and resolve any parity/formatting bugs.
- [x] Task incomplete
