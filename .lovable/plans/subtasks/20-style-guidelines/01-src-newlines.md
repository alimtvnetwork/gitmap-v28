# Subtask 20.01: Enforce Blank Lines in `src/`

## Target

- Directory: `src/` (.ts, .tsx)

## Violations to Fix

- [ ] Blank lines before `return` and `throw`.
- [ ] Blank lines after closing `}`.
- [ ] Blank lines before `if` statements.
- [ ] Remove double empty lines.

## Acceptance Criteria

- [ ] `check-newline-styling.py` passes with 0 errors on `src/`.
