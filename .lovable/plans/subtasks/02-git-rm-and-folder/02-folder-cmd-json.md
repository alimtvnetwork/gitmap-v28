# Subtask 2: JSON/YAML export for older

1. In gitmap/cmd/folder/, add logic for .json and .yaml extensions.
2. Build a recursive struct FolderNode to represent the tree.
3. Marshal and write to the output file.
