# Subtask 5: Search Commands & Caching

1. Register CLI commands: search, 
eplace, 
eplace-regex, 
eplace history, 
epo history, 
epo-search, 
epo-search-json, 
epo-search clear, search-replace-all clear, etc.
2. Implement the Analytical Cache in Repo DB (>50 identical queries gets cached).
3. Wire up all handlers.
