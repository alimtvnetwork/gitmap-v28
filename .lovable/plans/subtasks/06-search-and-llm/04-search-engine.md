# Subtask 4: Lazy Regex & Search Engine
1. Implement Lazy Regex compilation (wrap standard regexp to compile only on first use).
2. Implement search logic utilizing standard SQL LIKE where possible, and regex for complex queries.
3. Implement JSON/YAML output format for LLMs (Search Text, Position, Absolute/Relative paths).
