# Subtask 27-03: Subcommand Markers & Version Footers

## Goal

Provide discoverability markers (`▸ subcommands`) for multi-level commands and print clean build footers (`gitmap vX.Y.Z (build abc123)`).

## Status: DONE

- Implemented in `gitmap/cmd/rootusage.go` and `gitmap/cmd/rootusage_categories.go`.
