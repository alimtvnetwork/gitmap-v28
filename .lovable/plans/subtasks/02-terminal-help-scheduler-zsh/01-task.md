# Task 1: CLI Help Re-Alignment & Colors

- Modify `gitmap/cmd/rootusage*.go` and `constants_help*.go`.
- Add newline gaps before headers to provide breathing room.
- Apply colors (e.g., `constants.ColorCyan` or `ColorGreen`) to CLI flags (`--verbose`, `--interactive`, etc.).
- Fix the column alignment in the output sections (e.g., `clone-fix-repo-pub`, `interactive`).
- Insert missing help entries: SSH, SSH Join, Coding Guideline, Installer, Macro.
- Strictly adhere to 15-line max function size.
