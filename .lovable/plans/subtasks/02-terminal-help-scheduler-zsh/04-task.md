# Task 4: Ubuntu ZSH Installer

- Update `gitmap/cmd/setup.go` or create `setup_ubuntu.go`.
- Add an interactive/automated routine to install ZSH, configure defaults, and switch themes on Ubuntu.
- No generic garbage variables. 15-line maximum size per function.
