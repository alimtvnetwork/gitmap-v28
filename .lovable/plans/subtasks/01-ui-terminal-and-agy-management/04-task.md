# Task 4: Antigravity CLI Management

Implement `gitmap agy` commands (`add`, `rm`, `ls`, `stats`, `update`).
- Create `gitmap/cmd/agy_cmd.go`.
- Map the commands to manage `~/.gemini/config/projects/`.
- Must follow 15-line function limit.
