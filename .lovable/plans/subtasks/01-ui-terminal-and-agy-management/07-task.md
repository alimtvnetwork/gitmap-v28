# Task 7: Update Output Summary
Modify `gitmap/cmd/update.go` (and `release.go` if applicable) to output a nice summary at the end of the operation.
- The summary must explicitly state the previous version and the current version updated to (`vOLD -> vNEW`).
- It must print the last 2 lines of the changelog.
- It must add exactly 2 empty lines at the very end of the communication.
- Strictly adhere to 15-line max function size and strict boolean rules.
