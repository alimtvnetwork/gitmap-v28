# Task 2: UI Terminal Backend

Implement the backend WebSockets/SSE proxy for the UI Terminal.
- Hook it into `gitmap/cmd/hd_server.go` or equivalent.
- Handle terminal autocomplete requests.
- Wrap all errors with appropriate AppError structs.
